#include <stdio.h>
int main(){
    int contador = 0;
    while(contador<20){
        printf("%d: Um vetor eh uma classe de equipolencia de segmentos orientados\n", contador);
	contador++;
    }
    return 0;
}
